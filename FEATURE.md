feature: ring-universe
repos:   forage
branch:  claude/ring-universe
intent:  The ring as the whole universe on a thread. (1) the quote cascade drops
         out-of-ring quoters like replies, instead of drawing [removed] stubs;
         (2) one punch-hole setting, on by default: replies from outside the ring
         on a post from inside it; (3) the thread pill mutes a stop nobody on
         the thread belongs to. The feed/hashtag exemption STAYS as-is (owner,
         2026-09-04) — mixes, later, are what scope by default.
started: 2026-09-04
