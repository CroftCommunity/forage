{
  "name": "forage",
  "private": true,
  "version": "0.0.0",
  "description": "Forage — topic-driven aggregation in the Reddit structural family; behavior-scale build",
  "type": "module",
  "engines": {
    "node": ">=22 <23"
  },
  "scripts": {
    "test": "node --test test/"
  }
}
