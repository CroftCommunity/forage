{
  "name": "forage",
  "private": true,
  "version": "0.0.0",
  "description": "Forage \u2014 topic-driven aggregation in the Reddit structural family; behavior-scale build",
  "type": "module",
  "engines": {
    "node": ">=22 <23"
  },
  "scripts": {
    "test": "node --test",
    "conformance": "node conformance/run.js",
    "workflows": "node e2e/run.mjs"
  },
  "devDependencies": {
    "@playwright/test": "1.61.0",
    "@stryker-mutator/core": "^10.0.0"
  }
}