{
  "name": "forage",
  "private": true,
  "version": "0.0.0",
  "description": "Forage — topic-driven aggregation in the Reddit structural family; behavior-scale build",
  "type": "module",
  "engines": {
    "node": ">=22 <23"
  },
  "scripts": {
    "test": "node --test",
    "conformance": "node conformance/run.js",
    "workflows": "node e2e/run.mjs",
    "preview": "node scripts/preview.mjs",
    "import-phpbb": "node scripts/import-phpbb-style.mjs"
  },
  "devDependencies": {
    "@atproto/lexicon": "^0.7.12",
    "@axe-core/playwright": "^4.13.0",
    "@playwright/test": "^1.61.1",
    "@stryker-mutator/core": "^10.0.0"
  },
  "overrides": {
    "qs": ">=6.15.2"
  }
}
